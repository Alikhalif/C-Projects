#include <stdio.h>
#include <stdlib.h>

/*
int maximum(int Taille){
    static int Treturn[2];
    int i, Max, Min, T[100];
    for(i=0; i<Taille; i++){
        printf("donner nomber %d : ",i+1);
        scanf("%d",&T[i]);
    }

    Treturn[0]=T[0];
    Treturn[1]=T[0];
    for(i=0; i<Taille; i++){
        if(T[i] > Treturn[0]){
            Treturn[0] = T[i];
        }
        if(T[i] < Treturn[1]){
            Treturn[1] = T[i];
        }
    }

    return Treturn;
}

int main()
{
    int i, *Tmax, Min, Taille, T[100];
    printf("donner lataille tableau : ");
    scanf("%d",&Taille);

    Tmax=maximum(Taille);

    printf("le nomber Max est : %d  ",Tmax[0]);
    printf("le nomber Min est : %d  ",Tmax[1]);
}
*/

/////////////////////////////////////////////////////////////
int main(){



    retun 0;
}



